<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Aurora skin.
         Deep charcoal base with a single confident electric-teal accent. Semantic colour is reserved for up/down direction only, so nothing competes with the numbers.

         Themes carry colour and type only. Every control template lives in
         Controls.xaml and resolves these keys through DynamicResource, so
         switching skins restyles the whole app live and no two skins can drift
         apart structurally. Any new skin only has to define this same key set. -->

    <!-- Raw colours are exported alongside the brushes because the dashboard
         chart is drawn by SkiaSharp, which needs colour components rather than
         a WPF brush. -->
    <Color x:Key="BgColor">#0B0E13</Color>
    <Color x:Key="SurfaceColor">#12161D</Color>
    <Color x:Key="AccentColor">#2DD4BF</Color>
    <Color x:Key="UpColor">#FB923C</Color>
    <Color x:Key="DownColor">#38BDF8</Color>
    <Color x:Key="TextPrimaryColor">#F2F5F9</Color>
    <Color x:Key="TextMutedColor">#7E8899</Color>
    <Color x:Key="BorderColor">#242B36</Color>

    <!-- Base surfaces, lightest-on-top ordering: Bg &lt; Surface &lt; SurfaceAlt &lt; Elevated -->
    <SolidColorBrush x:Key="BgBrush" Color="#0B0E13"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="#12161D"/>
    <SolidColorBrush x:Key="SurfaceAltBrush" Color="#181D26"/>
    <SolidColorBrush x:Key="ElevatedBrush" Color="#1C222C"/>

    <!-- Hairlines. BorderStrong is the hover/active state of Border. -->
    <SolidColorBrush x:Key="BorderBrush" Color="#242B36"/>
    <SolidColorBrush x:Key="BorderStrongBrush" Color="#333C4A"/>

    <!-- Interaction states -->
    <SolidColorBrush x:Key="HoverBrush" Color="#1F2632"/>
    <SolidColorBrush x:Key="PressBrush" Color="#161B23"/>
    <SolidColorBrush x:Key="SelectedBrush" Color="#1E2A2E"/>

    <!-- Accent. OnAccentText is the contrast-safe foreground for anything
         sitting on top of a filled accent surface. -->
    <SolidColorBrush x:Key="AccentBrush" Color="#2DD4BF"/>
    <SolidColorBrush x:Key="AccentSoftBrush" Color="#1B3A38"/>
    <SolidColorBrush x:Key="OnAccentTextBrush" Color="#04211E"/>

    <!-- Direction colours: the only semantic hues in the palette, so speed
         reads instantly without a legend. -->
    <SolidColorBrush x:Key="UpBrush" Color="#FB923C"/>
    <SolidColorBrush x:Key="DownBrush" Color="#38BDF8"/>

    <!-- Text ramp: Primary for values, Secondary for prose, Muted for labels. -->
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#F2F5F9"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#B9C2CF"/>
    <SolidColorBrush x:Key="TextMutedBrush" Color="#7E8899"/>

    <SolidColorBrush x:Key="DangerBrush" Color="#F87171"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="#4ADE80"/>

    <!-- Segoe UI Variable is Windows 11's UI face and falls back cleanly to
         Segoe UI on Windows 10. Cascadia Mono keeps digits the same width so
         a changing speed reading never shifts the layout. -->
    <FontFamily x:Key="UiFont">Segoe UI Variable Text, Segoe UI</FontFamily>
    <FontFamily x:Key="MonoFont">Cascadia Mono, Consolas</FontFamily>

</ResourceDictionary>



