<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:svc="clr-namespace:GeeTM.Services">

    <!-- The "Premium" look overlay. Loaded ON TOP OF Controls.xaml (never
         instead of it) when Settings > Appearance > Skin is set to
         "Premium" - only the keys below are overridden; everything else in
         the app keeps using the base Controls.xaml definitions unchanged.
         Every color here uses the theme's own DynamicResource brushes/colors
         (AccentBrush, AccentColor, BorderStrongBrush, etc.), so this looks
         correct under any of the four color skins (Aurora/Midnight/Solar/
         Mono), not just one specific palette. -->

    <!-- Card: real layered depth via a soft drop shadow and a touch more
         corner rounding, instead of a flat single-color panel. -->
    <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderStrongBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="16"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="10"
                                  BlurRadius="26" Opacity="0.28" RenderingBias="Performance"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- NavButton: structurally identical to the base style (same named
         parts, same NavState.LabelVisibility binding that drives sidebar
         collapse, same triggers) - the only change is the active-state
         marker, which becomes a soft accent glow fading toward the bottom
         via an OpacityMask, rather than a flat solid-color bar. The mask
         doesn't touch the marker's actual color, which still comes from the
         theme's own AccentBrush. -->
    <Style x:Key="NavButton" TargetType="Button">
        <Setter Property="Height" Value="42"/>
        <Setter Property="Margin" Value="0,3"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="marker" Width="3" HorizontalAlignment="Left" Height="26"
                                CornerRadius="2" Background="{DynamicResource AccentBrush}" Opacity="0">
                            <Border.OpacityMask>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#FFFFFFFF" Offset="0"/>
                                    <GradientStop Color="#00FFFFFF" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.OpacityMask>
                        </Border>
                        <Border x:Name="fill" CornerRadius="9" Background="Transparent" Margin="6,0,0,0">
                            <Grid Margin="12,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="22"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="icon" Grid.Column="0" Text="{TemplateBinding Tag}"
                                           FontFamily="Segoe MDL2 Assets" FontSize="15"
                                           Foreground="{TemplateBinding Foreground}"
                                           VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <ContentPresenter x:Name="label" Grid.Column="1" Margin="12,0,0,0"
                                                  VerticalAlignment="Center"
                                                  Visibility="{Binding (svc:NavState.LabelVisibility), RelativeSource={RelativeSource TemplatedParent}}"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"
                                                  TextElement.FontFamily="{DynamicResource UiFont}"
                                                  TextElement.FontSize="13"
                                                  TextElement.FontWeight="Medium"/>
                            </Grid>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="fill" Property="Background" Value="{DynamicResource ElevatedBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="marker" Property="Opacity" Value="1"/>
                            <Setter TargetName="fill" Property="Background" Value="{DynamicResource ElevatedBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="label" Property="Opacity" Value="1"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- PrimaryButton: structurally identical to the base style (same
         template, same hover/pressed/disabled opacity triggers) - the only
         addition is a soft accent-colored glow beneath it, instead of
         sitting flat against the page. -->
    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnAccentTextBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="20,8"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="{DynamicResource AccentColor}" Direction="270"
                                  ShadowDepth="6" BlurRadius="20" Opacity="0.45" RenderingBias="Performance"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Opacity" Value="0.9"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Opacity" Value="0.7"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
