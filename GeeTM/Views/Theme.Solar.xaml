<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Solar skin.
         Warm near-black with an amber accent. Pairs with warm wallpapers where a cool teal would read as a foreign object.

         Themes carry colour and type only. Every control template lives in
         Controls.xaml and resolves these keys through DynamicResource, so
         switching skins restyles the whole app live and no two skins can drift
         apart structurally. Any new skin only has to define this same key set. -->

    <!-- Raw colours are exported alongside the brushes because the dashboard
         chart is drawn by SkiaSharp, which needs colour components rather than
         a WPF brush. -->
    <Color x:Key="BgColor">#121013</Color>
    <Color x:Key="SurfaceColor">#1A171B</Color>
    <Color x:Key="AccentColor">#FBBF24</Color>
    <Color x:Key="UpColor">#FB7185</Color>
    <Color x:Key="DownColor">#38BDF8</Color>
    <Color x:Key="TextPrimaryColor">#F7F3EE</Color>
    <Color x:Key="TextMutedColor">#918780</Color>
    <Color x:Key="BorderColor">#332D34</Color>

    <!-- Base surfaces, lightest-on-top ordering: Bg &lt; Surface &lt; SurfaceAlt &lt; Elevated -->
    <SolidColorBrush x:Key="BgBrush" Color="#121013"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="#1A171B"/>
    <SolidColorBrush x:Key="SurfaceAltBrush" Color="#221E23"/>
    <SolidColorBrush x:Key="ElevatedBrush" Color="#282329"/>

    <!-- Hairlines. BorderStrong is the hover/active state of Border. -->
    <SolidColorBrush x:Key="BorderBrush" Color="#332D34"/>
    <SolidColorBrush x:Key="BorderStrongBrush" Color="#463E48"/>

    <!-- Interaction states -->
    <SolidColorBrush x:Key="HoverBrush" Color="#282329"/>
    <SolidColorBrush x:Key="PressBrush" Color="#1D191E"/>
    <SolidColorBrush x:Key="SelectedBrush" Color="#33281C"/>

    <!-- Accent. OnAccentText is the contrast-safe foreground for anything
         sitting on top of a filled accent surface. -->
    <SolidColorBrush x:Key="AccentBrush" Color="#FBBF24"/>
    <SolidColorBrush x:Key="AccentSoftBrush" Color="#3B2E12"/>
    <SolidColorBrush x:Key="OnAccentTextBrush" Color="#241900"/>

    <!-- Direction colours: the only semantic hues in the palette, so speed
         reads instantly without a legend. -->
    <SolidColorBrush x:Key="UpBrush" Color="#FB7185"/>
    <SolidColorBrush x:Key="DownBrush" Color="#38BDF8"/>

    <!-- Text ramp: Primary for values, Secondary for prose, Muted for labels. -->
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#F7F3EE"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#C9C0B8"/>
    <SolidColorBrush x:Key="TextMutedBrush" Color="#918780"/>

    <SolidColorBrush x:Key="DangerBrush" Color="#F87171"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="#A3E635"/>

    <!-- Segoe UI Variable is Windows 11's UI face and falls back cleanly to
         Segoe UI on Windows 10. Cascadia Mono keeps digits the same width so
         a changing speed reading never shifts the layout. -->
    <FontFamily x:Key="UiFont">Segoe UI Variable Text, Segoe UI</FontFamily>
    <FontFamily x:Key="MonoFont">Cascadia Mono, Consolas</FontFamily>

</ResourceDictionary>



